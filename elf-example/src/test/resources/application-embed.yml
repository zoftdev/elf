elf:
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}
spring:
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}
    consumer:
      group-id: embed-test-elf
      auto-offset-reset: earliest
    listener:
      concurrency: 1
